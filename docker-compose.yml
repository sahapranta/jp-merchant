services:
  app:
    build: .
    ports:
      - "8080:8080"
    environment:
      - SPRING_DATASOURCE_URL=jdbc:h2:file:/data/edubilldb;MODE=MySQL;AUTO_RECONNECT=TRUE
    volumes:
      - h2-data:/data

volumes:
  h2-data:
